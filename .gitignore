node_modules/
.next/
dist/
build/
*.log

# Build artifacts (keep out of git — large binaries)
*.apk
*.aab
*.ipa
dist-android/
.expo/

# Locally generated Expo native project (regenerated by `expo prebuild` / eas build)
apps/mobile/android/

# Scratch / one-off test scripts (not source)
apps/api/src/test-env.ts

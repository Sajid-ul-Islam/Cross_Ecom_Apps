# Local environment overrides (copy to `.env` and fill in — `.env` is gitignored).
# The gateway API key is injected at build time via EXPO_PUBLIC_GATEWAY_API_KEY.
# EAS builds: set this as a project secret instead (it is NOT read from this file in CI):
#   eas secret:create --scope project --name EXPO_PUBLIC_GATEWAY_API_KEY --value "..."
EXPO_PUBLIC_GATEWAY_API_KEY=fa002b126085801f23d9375d94409752503639919e39690c42877fc58c624973
